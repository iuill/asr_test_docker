# ReazonSpeech k2-v2 Base Image - CPU Version
# Contains: Common base + sherpa-onnx

FROM reazonspeech-common-base:cpu

# Install sherpa-onnx (CPU version)
RUN --mount=type=cache,target=/root/.cache/pip \
    pip install sherpa-onnx
